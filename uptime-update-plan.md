# uptime-copilot 업데이트 계획 — 코드 리뷰 개선 사항 반영

> 작성일 2026-09-23 (개정: MRO Copilot 업그레이드 내용 분리) · 대상 저장소: `github.com/surplus96/uptime-copilot` (기준 커밋 `6514038`)
> 근거: `claude/specguard-research-and-uptime-review.md` PART 2 (코드 리뷰 개선점 ①~⑪)
> 범위: **리뷰에서 나온 개선 사항만** 다룬다. MRO Copilot으로 확장하기 위한 추가 구성(부품·재고, 오프라인 모드, 감사 로그, 자산 어댑터 등)은 `claude/mro-copilot-upgrade-plan.md`에서 다룬다.
> 순서(확정): **이 문서(U0~U2, ~10/11)** → MRO Copilot 업그레이드(~10/25) → SpecGuard(10/26~)
> 개발 환경: MacBook Pro Apple Silicon, RAM 16GB, Ollama 설치됨 · 주 15~20시간
> 진행 방식: 본인이 직접 구현. 각 작업은 **목표 · 변경 위치 · 완료 기준(DoD)** 으로 정의한다 (코드는 포함하지 않음)
> **헬퍼 모델은 아래 "🧭 개발 헬퍼 모델 운영 지침"을 먼저 읽고 따른다.**

---

## 🧭 개발 헬퍼 모델 운영 지침 (Sonnet 5용 — 작업 시작 전에 반드시 읽을 것)

> 이 장은 이 문서를 받아 개발을 돕는 **헬퍼 모델(기본: Claude Sonnet 5)** 에게 주는 지침이다. 헬퍼는 작업 중 이 지침에 따라 사용자에게 **안내·경고·중단**을 해야 한다.
> 배경: Sonnet 5는 이 계획 작업의 대부분(표준 스택 구현·테스트·리팩터링)에 충분하다. 다만 ① 미묘한 설계·통계 오류를 놓칠 수 있고 ② **도메인 사실 오류는 어떤 모델을 써도 사라지지 않는다.** 그래서 정해진 시점에만 Opus급 모델로 교차 검토하고, 도메인 사실은 모델이 아니라 **절차**로 막는다.

### H1. 작업 진행 규칙

1. **한 번에 작업 ID 하나만** 진행한다 (예: `1-2`, `MRO-FR-03`, `FR-05`). 시작할 때 해당 작업의 목표와 완료 기준(DoD)을 한 줄로 다시 말하고 시작한다.
2. **교육 모드**: 사용자가 직접 타이핑하며 이해하는 방식이다. 전체 코드를 완성해 주지 말고 **단계별 설명 + 참고 코드 조각**을 제시한다. 사용자가 명시적으로 요청할 때만 전체 구현을 보여준다.
3. **완료 선언 조건**: DoD를 명령 실행 결과(테스트, 린터, 평가 수치)로 확인하기 전에는 "완료"라고 말하지 않는다. 확인하지 못했으면 "미확인"이라고 명시한다.
4. **범위 고정**: "선택"으로 표시된 작업은 사용자가 지시할 때만 시작한다. 새 아이디어는 작업하지 말고 문서의 "이후" 목록에 제안만 한다.
5. **커밋·푸시·외부 전송**은 사용자가 요청할 때만 한다.
6. 계획과 다르게 진행해야 하면 먼저 차이와 이유를 알리고 승인을 받는다.

### H2. 교차 검토 운영 (Sonnet 5 → Opus급)

**정기 체크포인트**: 이 문서의 🔶 표시(H4 체크포인트 표와 각 주차 DoD 옆)에 도달하면 헬퍼는 다음 작업으로 넘어가지 말고 아래 절차를 따른다.

1. 사용자에게 알린다: `🔶 교차 검토 체크포인트 [ID] 도달 — 다음 단계 진행 전 Opus급 모델 검토를 권장합니다.`
2. **검토 패키지**를 작성해 제공한다 (사용자가 복사해서 상위 모델에 전달하는 용도):
   - 배경 요약 (5줄 이내): 무엇을 왜 만들었는가
   - 검토 대상: 파일 경로·핵심 코드 발췌 또는 diff, 산출된 수치
   - 체크포인트 표의 **검토 질문 목록** + 헬퍼 스스로 확신이 낮은 지점
   - 전제·가정 목록
3. 검토 결과를 사용자가 가져오면 `docs/decisions.md`(결정 기록)에 **날짜 · 체크포인트 ID · 지적 사항 · 조치 · 미해결 항목**을 남기고, 지적 사항을 해결한 뒤 다음 작업으로 진행한다.
4. 헬퍼는 **자기 작업을 스스로 "검토 통과"로 판정하지 않는다.**

**수시 에스컬레이션 트리거** — 체크포인트가 아니어도 아래 상황이면 즉시 알리고 교차 검토를 제안한다.
- 같은 버그를 두 번 고쳤는데도 재현될 때
- 결과가 **너무 좋을 때** (예: 재현율이 기준선보다 0.3 이상 급등, 정확도 1.0) → 누수·평가 오류를 먼저 의심
- 비동기 루프·동시성·트랜잭션 관련 코드를 수정할 때
- README·케이스 스터디에 들어갈 수치나 주장을 새로 만들 때
- 되돌리기 어려운 변경 (DB 스키마 변경, 데이터 삭제, 이력 재작성)

### H3. 모델을 바꿔도 해결되지 않는 위험 — 도메인 사실 프로토콜

**위험**: 규칙 조항 번호, 개정판, 규제 수치 한도, 제출도서 항목, 장비 파라미터, 정비 절차는 모델이 **그럴듯하지만 틀리게** 말할 수 있다. 상위 모델로 바꿔도 이 위험은 남는다. 이 오류는 테스트를 통과한 상태로 결과물에 들어가서 포트폴리오의 신뢰를 무너뜨린다.

**헬퍼가 지킬 규칙**
1. 규제·규격·장비 관련 수치나 조항을 **기억에 의존해 사실처럼 쓰지 않는다.** 이번 세션에서 사용자가 제공한 원문이 없으면 `[미검증]` 태그를 붙이고, 확인 방법(어느 문서의 어느 부분을 볼지)을 함께 안내한다.
2. 도메인 데이터가 필요하면 먼저 사용자에게 **원문 발췌(문서명·판본·페이지 포함)** 를 요청한다. 헬퍼는 붙여 넣은 원문을 **요약·구조화만** 한다.
3. 레지스트리·마스터 데이터 항목은 `source`(문서명), `edition`(판본), `location`(페이지·조항), `verified_by`(검증자) 필드가 없으면 만들지 않는다. `verified_by`는 **사용자만** 채운다.
4. 테스트에 쓰는 규제 수치는 테스트 코드에 직접 적지 말고 **레지스트리·마스터 데이터에서 읽어 온다.** 그래야 원문이 바뀌거나 오류가 발견됐을 때 한 곳만 고치면 된다.
5. **개발 중 인용 가드**: 코드·문서·프롬프트에 등장하는 조항 ID는 레지스트리에 존재해야 한다. 헬퍼는 새 조항 ID가 등장하면 레지스트리 존재 여부를 확인하고, 없으면 경고한다.
6. 합성·가정 데이터는 파일과 README에 **합성/가정임을 명시**한다. 실제 데이터처럼 서술하지 않는다.
7. 확신이 없으면 "모른다"고 말하고 확인 절차를 제안한다. 추측으로 빈칸을 채우지 않는다.

### H4. 이 문서의 교차 검토 체크포인트

| ID | 시점 | 검토 질문 |
|---|---|---|
| ⚠️ 상시 (U0) | U0 작업 전반 | 정기 체크포인트는 없다. 대신 헬퍼가 **깨끗한 환경 재현 확인**을 직접 수행한다: 새 venv, `.env` 없음 상태에서 CI와 같은 명령(ruff, mypy, pytest fast path)을 실행하고 결과를 보고한다. 수치 정정(0-4)과 부작용 순서 수정(⑧)에는 H2 수시 트리거를 적용한다 |
| 🔶 CP-U1 | U1 모델 평가 직후, 에이전트 통합(3-6) **전** | 라벨 창(24h)이 고장 시각과 정확히 정렬되는가? 이동 통계·오류 횟수·교체 경과시간 피처에 미래 정보가 섞이지 않았는가? 시간 분할 경계에서 누수가 없는가? 같은 설비가 학습·테스트에 걸쳐 있는 영향은? Z-score 기준선과 공정하게 비교했는가(같은 기간·같은 단위)? 임계값 선택이 테스트셋을 보고 결정되지 않았는가? |
| 🔶 CP-U2 | U2 `priority_rule` 설계 확정 시 | 관점 출력 → 우선순위·정지 권고 규칙에 모순이나 누락된 조합은 없는가? LLM 출력이 비정상(누락·범위 밖 값)일 때 안전한 기본값으로 처리되는가? 규칙이 결정론적이고 테스트 가능한가? |
| 🔶 CP-U3 | U2 종료, README·케이스 스터디 갱신 직전 | 문서의 모든 수치가 저장소 명령으로 재현되는가? Before/After 표가 같은 조건에서 측정되었는가? 과장된 주장은 없는가? |

**이 문서의 도메인 사실 주의 지점**: 부품별 정비 절차(`pump_manual.py`)는 "원심펌프 가정"의 교육용 시나리오다. 절차를 추가·수정하면 H3 규칙에 따라 출처를 남기고, 출처가 없으면 가정임을 표시한다.

---

## 1. 개선 항목 총괄

| 리뷰 번호 | 개선 항목 | 우선순위 | 단계 | 예상 시간 |
|---|---|---|---|---|
| ① | 테스트 2건 실패 + ruff 1건 → CI 복구 | 🔴 P0 | U0 | 1시간 |
| ② | 케이스 스터디 수치 불일치 (51 vs 49 테스트, "9 리뷰 에이전트") | 🔴 P0 | U0 | 30분 |
| ⑦ | 설비 번호가 없어도 번호를 지어냄 | 🟡 P2 | U0 | 2시간 |
| ⑧ | 외부 전송이 출력 검증보다 먼저 실행됨 | 🟡 P2 | U0 | 2시간 |
| ⑨ | 판정 실패 시 fail-open, 죽은 코드 | 🟡 P2 | U0 | 1시간 |
| ⑩ | import 시점 부작용, print, 디버그 파일, 문서 위치, 커밋 이력 | 🟡 P2 | U0 | 3시간 |
| ⑥ | 정량 평가 부재 → 골든셋 + 평가 실행기 | 🟠 P1 | U0 | 반나절 |
| ③ | 예측이 실제 분포에서 거의 동작하지 않음 → 고장 위험 모델 | 🟠 P1 | U1 | 3~4일 |
| ④ | 관점 노드가 결정에 쓰이지 않음 → 구조화 출력 + 우선순위 규칙 | 🟠 P1 | U2 | 1.5일 |
| ⑤ | RAG가 에이전트와 분리, 과잉설계 → 역할 축소 | 🟠 P1 | U2 | 반나절 |
| ⑪ | OpenAI 전용 → LLM 제공자 어댑터 (Ollama 전환) | 🟡 P2 | U2 | 1일 |

### 일정

| 단계 | 기간 | 내용 |
|---|---|---|
| U0 | 9/24 – 9/27 | ①② 복구 → ⑦⑧⑨⑩ 결함·위생 → ⑥ 평가 기반 (추석 연휴 포함, 유동적) |
| U1 | 9/28 – 10/4 | ③ 고장 위험 예측 모델 (개천절 포함) |
| U2 | 10/5 – 10/11 | ④ 관점 구조화 → ⑤ RAG 정리 → ⑪ 제공자 어댑터 → README 갱신 (한글날 포함) |

---

## 2. U0-a · 깨진 것 복구 (①②)

| ID | 작업 | 변경 위치 | 완료 기준 |
|---|---|---|---|
| 0-1 | CMMS 테스트 실패 수정: 테스트가 `CMMS_MCP_URL`을 monkeypatch하도록 하거나 URL을 함수 인자로 주입 | `tests/test_cmms_client.py` 또는 `cmms_client.py:32` | `.env` 없이 `pytest tests/ --ignore=tests/test_rag_dedup.py` 전부 통과 |
| 0-2 | ruff E402 수정 (import 순서 정리 또는 사유 주석을 단 noqa) | `rag/generate_docs.py:20` | `ruff check .` 0건 |
| 0-3 | GitHub Actions 결과 확인 + README에 CI 배지 추가 | README 두 벌 | 배지 녹색 |
| 0-4 | 케이스 스터디 수치 정정: 테스트 수를 실측값으로, "9 전담 리뷰 에이전트"는 헤드라인 배지에서 내려 "개발 프로세스" 섹션으로 이동 | `docs/PORTFOLIO*.md` | 문서의 모든 수치를 저장소에서 재현 가능 |

**원인과 교훈**: mypy 도입 때 추가한 `assert`가 `.env` 없는 환경에서 테스트를 깨뜨렸다. 로컬 `.env`가 문제를 가렸다. → 깨끗한 환경에서 CI와 같은 명령을 돌리는 습관을 만든다 (H4 상시 항목).

---

## 3. U0-b · 코드 결함과 위생 (⑦⑧⑨⑩)

### 3-1. 결함 수정

| ID | 문제 | 수정 방향 | 완료 기준 |
|---|---|---|---|
| 1-1 (⑦) | `IncidentExtraction.machine_id: int`가 필수라서 번호가 없어도 정수를 지어냄. 진단·정비일정 두 노드에 추출 코드 중복 (`agent_service.py:63-64, 174-183, 226-235`) | Optional로 바꾸고, None이면 "몇 번 설비인가요?"로 되묻는 분기 추가. 추출 로직을 헬퍼 하나로 통합 | "설비가 이상해" 입력 시 되묻기, 회귀 테스트 1개 |
| 1-2 (⑧) | `resume_agent`에서 `finalize_node`가 Slack·CMMS 전송을 먼저 하고 그 뒤에 `_validate_output` 실행 (`:354-362`, `:460-464`) | 출력 검증을 작업지시서 확정 직후(승인 요청 전)로 이동. 외부 전송은 검증 통과 + 승인 후에만 | "검증 실패 시 외부 호출 0회" mock 테스트 |
| 1-3 (⑨) | 충실도 판정이 두 번 실패하면 `pass: True` (`harness.py:146, 187`). `judge_response_quality`는 호출되지 않음 | 결과에 `verified: false` 표시, UI에 "검증되지 않음" 노출. 죽은 코드 삭제 | 판정 실패 경로 테스트 1개 |

### 3-2. 테스트 가능성과 위생 (⑩)

| ID | 작업 | 완료 기준 |
|---|---|---|
| 1-4 | import 시점 부작용 제거: `pdm_operations`의 CSV 로딩, `agent_service`의 OpenAI 클라이언트 생성을 지연 초기화로 | API 키와 데이터 없이 모듈 import 가능 |
| 1-5 | 가짜 LLM 클라이언트(고정 응답)로 그래프 라우팅 테스트: 긴급→승인 대기, 주의→작업지시서, 일반→종료 | 세 경로 테스트가 CI에서 실행 |
| 1-6 | `print()` 14곳을 `logging`으로 교체 | `grep print(` 0건 (스크립트 제외) |
| 1-7 | `backend/debug_comp3_context.txt` 삭제, 루트의 `PHASE_7_PLAN.md`·`SIMULATOR_PLAN.md`와 `frontend/UI_UPGRADE_PLAN.md`를 `docs/design/`으로 이동하고 링크 갱신 | 루트에 설계 문서 없음 |
| 1-8 | `docs/decisions.md`(결정 기록) 생성 — 이후 교차 검토 결과를 여기에 기록 | 파일 존재 |
| (보류) | 초기 커밋 메시지(`.`, `0916_commit_02` 등) 정리 | 기본값: 이력을 유지하고 정리하지 않음 |

---

## 4. U0-c · 정량 평가 기반 (⑥)

| ID | 작업 | 완료 기준 |
|---|---|---|
| 2-1 | 골든셋 40문항: 라우팅 정답 카테고리, 추출 정답 설비 번호(없음 포함), 기대 긴급도 | `tests/eval/golden.jsonl` |
| 2-2 | 평가 실행기: `pytest -m eval`(실제 LLM 호출, CI 제외)로 라우터 정확도, 추출 정확도, 되묻기 정확도 산출, 결과를 파일로 저장 | README에 기준선 표 |

이 실행기와 결과 형식은 이후 ③(모델 평가 기록), ⑪(로컬/클라우드 비교), MRO 업그레이드, SpecGuard에서 재사용한다.

**U0 완료 정의**: CI 녹색, 문서 수치 재현 가능, 결함 3건 수정 + 회귀 테스트, 골든셋 기준선 수치 확보.

---

## 5. U1 · 고장 위험 예측 모델 (③) ★

### 5-1. 문제

실제 고장 직전 24시간의 신호 편차는 평균 1.6σ이고, 3σ 이상은 1.2%뿐이다(`SIMULATOR_PLAN.md` 실측). 그런데 `detect_anomaly`의 임계값은 3σ이다. 실제 분포에서 '주의' 탐지 재현율은 약 1%이고, 데모는 `STRONG_FRACTION=0.15`로 강한 열화를 부풀려 보여준다. → "예지보전"이라는 핵심 주장이 실제 데이터에서 성립하지 않는다.

### 5-2. 모델 설계

| 항목 | 설계 |
|---|---|
| 예측 대상 | 설비 × 시각별 "향후 24시간 내 compN 고장" (옵션 48시간) |
| 형태 | 부품별 이진 분류 4개로 시작. 다중 클래스는 비교 실험으로 |
| 라벨 | `PdM_failures.csv`의 고장 시각 이전 24시간 구간 = 양성 |
| 피처 | 4개 신호의 3h·24h 이동 평균·표준편차 / 최근 24h 오류 유형별 횟수 / 부품별 마지막 교체 후 경과시간(maint) / 모델 종류·사용 연수 |
| 샘플링 | 3시간 간격 샘플 + 정상 클래스 다운샘플링 또는 클래스 가중치 |
| 분할 | **시간 기준**: 1~8월 학습 / 9~10월 검증(임계값 결정) / 11~12월 테스트. 설비 기준 분할은 부가 실험 |
| 모델 | LightGBM 기본값으로 시작 → 검증셋 기준 소폭 튜닝 |
| 설명 | 예측 건별 기여 피처 상위 3개 → 작업지시서 근거 문장 |
| 임계값 | "설비당 하루 오경보 ≤ N건" 제약 아래 재현율 최대화, 근거를 모델 카드에 기록 |

### 5-3. 16GB 메모리 운용

- 피처 테이블(약 87만 행 × 30여 컬럼)은 float32로 저장
- 이동 통계는 설비별로 계산해 Parquet로 캐시 (전체를 한 번에 메모리에 올리지 않음)
- 학습 중에는 Ollama를 끔
- 학습 중 최대 메모리 사용량을 기록해 모델 카드에 남김

### 5-4. 작업

| ID | 작업 | 완료 기준 |
|---|---|---|
| 3-1 | 데이터 기간·고장 분포 탐색, 라벨 정의 확정 | 탐색 노트 |
| 3-2 | 피처 파이프라인 (Parquet 캐시) | 재실행 시 동일 결과 |
| 3-3 | 학습 + 시간 분할 평가 + Z-score 기준선 비교 | 비교표 |
| 3-4 | 모델 카드 `docs/model_card.md` (데이터, 분할, 지표, 임계값 근거, 한계) | 문서 |
| — | 🔶 **CP-U1 교차 검토** | `decisions.md` 기록 |
| 3-5 | `predict_failure_risk(machine_id)` 도구: 부품별 확률 + 기여 피처 상위 3개 | 단위 테스트 (모델 파일 없는 CI에서는 fixture 사용) |
| 3-6 | 긴급도 규칙 개정: 실제 고장 기록 = 긴급(유지), **위험도 ≥ 임계값 = 주의**, Z-score는 보조 근거로 | 시나리오 테스트 갱신 |
| 3-7 | 작업지시서 [증상]에 "모델 추정 24시간 내 고장확률 N%, 주요 근거: …" 추가 (값은 코드가 채움) | 결정론 원칙 유지 |
| 3-8 | 시뮬레이터의 `STRONG_FRACTION`을 실측 수준으로 낮춘 **현실화 모드**에서 Z-score vs 모델 탐지율 비교 | Before/After 표 |

### 5-5. 공개할 지표

| 지표 | Z-score 기준선 | 모델 |
|---|---|---|
| 부품별 재현율 / 정밀도 | | |
| PR-AUC | – | |
| 평균 조기 경보 시간 (고장 몇 시간 전 첫 경보) | | |
| 설비당 하루 오경보 수 | | |
| 현실화 모드 시뮬레이터 탐지율 | | |

**U1 완료 정의**: 테스트 기간 비교표 공개 (개선폭이 작아도 그대로 보고), 현실화 모드 동작, CP-U1 지적 사항 해결.

🔶 **CP-U1**: 3-4 완료 후 3-5 착수 전에 교차 검토. 재현율이 급등했다면 누수부터 의심한다.

---

## 6. U2 · 에이전트·RAG·LLM 구조 정리 (④⑤⑪)

### 6-1. 관점 노드를 결정에 반영 (④)

**문제**: 안전·생산·정비 3개 관점 노드가 150토큰 자유 텍스트를 만들지만, 어떤 결정에도 쓰이지 않고 승인 화면에 표시만 된다.

| ID | 작업 | 완료 기준 |
|---|---|---|
| 4-1 | 세 관점 노드를 구조화 출력으로: `risk_level`, `recommended_window`, `requires_shutdown`, `rationale` | Pydantic 스키마, 비정상 출력 시 안전한 기본값 |
| 4-2 | `priority_rule` 노드: 세 관점 + 모델 위험도를 **규칙으로** 종합 → 우선순위 P1~P3, 정지 권고 | 결정론 규칙 + 단위 테스트 |
| 4-3 | 관점 결과가 결정을 바꾸는 시나리오 5개 테스트 (예: 안전 high → 즉시 정지 권고) | 테스트 통과 |
| 4-4 | 작업지시서에 [우선순위]·[정지 권고]·[근거 관점] 블록 추가 | 승인 화면 표시 |

🔶 **CP-U2**: 4-2 규칙 설계 확정 시 교차 검토.

### 6-2. RAG 역할 정리 (⑤)

**문제**: 에이전트는 RAG를 쓰지 않고 매뉴얼 dict를 직접 조회한다. RAG 코퍼스는 생성된 txt 3개(청크 수 개)인데 Multi-Query + Hybrid + Self-RAG 3단 파이프라인이 얹혀 있어 과잉설계로 보인다.

| ID | 작업 | 완료 기준 |
|---|---|---|
| 5-1 | `/rag/query`를 "매뉴얼 Q&A 보조 기능"으로 역할 명시, Multi-Query·Self-RAG 계층 제거 (하이브리드 검색 + 충실도 판정만 유지) | 관련 테스트 갱신 |
| 5-2 | 축소 이유(코퍼스 규모 대비 과잉, RAG 역량은 SpecGuard에서 시연)를 설계 기록에 남김 | `decisions.md` |

### 6-3. LLM 제공자 어댑터 (⑪)

**문제**: OpenAI 전용이다. OpenAI SDK를 쓰고 있어서 `base_url`만 바꾸면 Ollama로 전환할 수 있지만, `reasoning_effort="none"` 같은 파라미터는 호환 엔드포인트에서 지원되지 않을 수 있다.

| ID | 작업 | 완료 기준 |
|---|---|---|
| 11-1 | 제공자 어댑터: OpenAI / Ollama(OpenAI 호환) 전환을 `.env` 한 줄로, 제공자별 미지원 파라미터 처리, 구조화 출력 실패 시 재시도 1회 | 어댑터 단위 테스트 |
| 11-2 | 모든 LLM 호출(`agent_service`, `harness`, `rag_service`)을 어댑터 경유로 변경 | 직접 `OpenAI(...)` 생성 0건 |
| 11-3 | 로컬(Ollama Qwen3-8B) vs 클라우드 골든셋 비교: 정확도·지연 | README 표 |

### 6-4. README·케이스 스터디 갱신

- CI 배지, 골든셋 기준선 표, 모델 비교표, 현실화 모드 Before/After, 로컬/클라우드 비교표
- 헤드라인 지표를 제품 지표로 교체 (예측 재현율 개선폭, 조기 경보 시간, 테스트 수 실측값)
- 케이스 스터디에 새 장: "측정해 보니 예측이 약했다 → 모델로 개선 → 과장 제거 후에도 동작"

🔶 **CP-U3**: README·케이스 스터디 갱신 직전 교차 검토.

**U2 완료 정의**: 관점 출력이 우선순위를 실제로 바꾸고, RAG 역할이 정리되었으며, `.env` 한 줄로 로컬 모델 전환이 가능하고, 문서 수치가 모두 재현된다.

---

## 7. 체크리스트

**U0**
- [x] 0-1 CMMS 테스트 · [x] 0-2 ruff · [x] 0-3 CI 배지 · [x] 0-4 수치 정정
- [x] 1-1 되묻기 · [x] 1-2 검증→전송 순서 · [x] 1-3 fail-open 표시
- [x] 1-4 지연 초기화 (CI 긴급 대응으로 순서 당김) · [x] 1-5 그래프 테스트 · [x] 1-6 logging · [x] 1-7 파일 정리 · [x] 1-8 decisions.md
- [x] 2-1 골든셋 · [x] 2-2 평가 실행기 + 기준선 표 (라우터 90.0% / 추출 100.0% / 되묻기 100.0%, 2026-09-23)
- [x] 깨끗한 환경 재현 확인 결과 보고 — CI(GitHub Actions, 매 push마다 archive/ 없음 + API 키 없음 상태로 실행)가 이 역할을 계속 수행 중, 전 커밋 그린

**U1**
- [x] 3-1 ~ 3-4 모델 학습·평가·모델 카드 → [x] 🔶 CP-U1 (2026-09-23 조기 발동, 상위 모델 교차 검토 완료 — 미래 데이터 누수 없음 확인, `docs/decisions.md` 참고) → [x] 3-5 ~ 3-8 통합·현실화 모드 (2026-09-23 전부 완료, U1 종료)

**U2**
- [x] 4-1 ~ 4-4 관점 구조화·우선순위 (🔶 CP-U2, 2026-09-23 완료 — 상위 모델 교차 검토로 규칙 재설계, `docs/decisions.md` 참고) · [x] 5-1 ~ 5-2 RAG 정리 (2026-09-23 완료) · [x] 11-1 ~ 11-3 제공자 어댑터 (2026-09-23 완료 — 실측: 라우터 90.0%→77.5%, 지연 10배+, `docs/decisions.md`/README 참고) · [x] README·케이스 스터디 갱신 (🔶 CP-U3, 2026-09-23 완료 — 상위 모델 교차 검토로 헤드라인 지표 오류 발견·수정, `docs/decisions.md` 참고) — **U2 종료**

---

## 8. 다음 단계

U2 완료 정의를 충족하면 `claude/mro-copilot-upgrade-plan.md`로 넘어간다. 이 문서의 산출물 중 **고장 위험 도구(3-5), 우선순위 규칙(4-2), 제공자 어댑터(11-1), 평가 실행기(2-2)** 가 MRO 업그레이드의 전제 조건이다.